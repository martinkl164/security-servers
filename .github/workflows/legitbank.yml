name: LegitBank CI (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated workflow
    runs-on: ubuntu-latest
    steps:
      - name: Message
        run: |
          echo "This workflow is deprecated."
          echo "Use legitbank-pr.yml for PRs and legitbank-production.yml for production deploys."